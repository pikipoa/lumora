# CHANGELOG

重要な修正・仕様変更のうち、**コードを読んだだけでは経緯が分からないもの**を記録する。
「なぜこんな作りになっているのか」「なぜこのチェックがあるのか」を半年後の自分が
辿れるようにするためのファイル。日常的な機能追加まで全部書く必要はない。

---

## 2026-07-27 — マーカー位置に前後の文脈を併せて保存し、3段階で解決する

### きっかけ

スマホで「同じ文字の別の場所にMarkerが記録される」報告。これを機に、保存時ガードの
**構造的な穴**に気づいた。

```ts
content.slice(start, end) === quoted_text
```

この検証は「テキストが違う」ズレは止められるが、**「テキストは同じで出現箇所が違う」
ズレは原理的に検出できない**。「Gemini」が6回出てくる会話で3つ目を選んでも、1つ目の
位置が計算されていれば、どちらも文字列は "Gemini" なので検証を通過してしまう。

### 対処：offsetに加えて前後40文字を保存し、3段階で解決する

| matchType | 条件 | UIでの扱い |
|---|---|---|
| `exact` | offsetが正しく文脈も一致／本文中に1箇所のみ | 通常表示 |
| `context` | offsetは使えないが文脈から特定できた | **何も出さない**（自動復元でユーザー操作に影響しないため） |
| `text_only` | 複数箇所あるが決め手がない | 注意表示（UIは未実装） |
| `missing` | 本文中に見つからない | 「再選択してください」案内（UIは未実装） |

「位置を推測して復元しました」を常時出すと通常利用で通知が増えすぎるため、`context`は
ログのみでUIには出さない。通知するのは本当に曖昧な`text_only`と`missing`だけにする。

### バックフィルの設計上の急所

**offsetと文脈では、間違えた時の被害が違う。**

- 誤ったoffsetは自己修復できる（文脈があれば段階2が訂正する）
- **誤った文脈は自己修復できない**。段階2がその文脈を信じて、自信を持って間違った箇所を
  選び続ける。一度焼き付けると永久に間違ったままになる

したがって**文脈を生成してよいのは、位置が確実に分かっているマーカーだけ**。
`start_offset`が`null`のマーカーから文脈を作ると結局`indexOf`に頼ることになり、本当は
3つ目だったマーカーに1つ目の文脈を焼き付けてしまう。`scripts/backfill/`はこの原則に沿って
「文脈を付ける対象」と「offsetを付ける対象」を別スクリプトに分けている。

あわせて、一意性の判定に`replace()`の長さ差を使うと**重なり合う出現を数え落とす**ことも
判明した（本文`"あああ"`の中の`"ああ"`は2箇所あるが1回と判定される）。「最初の一致より
後ろにもう1つ一致があるか」で判定する方式に変更した。

### 既存マーカーのTier 2について

複数箇所に出現する既存マーカーは、**データからは復元不能**。文脈を保存していないため、
どの出現箇所だったかを示す情報がどこにも残っていない。放置／引き直し／削除の選択肢を
`scripts/backfill/README.md`に整理した。新規マーカーでは発生しない。

---

## 2026-07-26（追記・結論） — 位置ズレの真因はブラウザ拡張機能によるDOM書き換えだった

下の項目は当初「アプリ側のバグ」として調査した記録だが、**最終的な原因はLumoraの
コードではなく、ユーザーのブラウザ拡張機能がページのDOMを書き換えていたこと**だった。
シークレットウィンドウ（拡張機能が無効）で同じ操作を行うと正常に動作することで確定した。

### 決め手になったデータ

保存前ガードが止めた時点の診断で、次が同時に成立していた：

| 項目 | 値 |
|---|---|
| レンダー時に使った`content`の長さ | 1835（正しい） |
| DOMの実テキスト長 | 975 |
| `computeSegments`が返すセグメント数 | 27（合計1835文字） |
| DOMに実在するセグメント要素数 | 39（合計975文字、うち**11個が空**） |
| DOM側`data-seg-start`の並び | 重複・順序入れ替わりあり |

Reactは正しく描画しているのに、DOMには**Reactが作っていない余分な要素・空要素・
重複・順序の入れ替わり**が存在した。そして決定的だったのは**単語そのものが違っていた**
こと（DOM「得意分野が違うため使用している」／content「異なるため使い分けている」）。
Lumoraのコードに文章を言い換える処理は存在しないため、外部がDOMを書き換えていると確定した。

翻訳・文章校正系の拡張機能はページのテキストを言い換え、改行を落とし、元要素を空のまま
残す。観測された症状と完全に一致する。

### 対処

拡張機能はアプリ側から制御できない。**検出して保存を止め、原因をユーザーに伝える**のが
唯一の正しい対処になる。保存前ガード（`content.slice(start,end) === quoted_text`）は
そのまま残し、失敗時のメッセージで拡張機能の可能性を案内する。Sentryにも記録する。

### 調査の進め方についての反省

React内部（メモ化、多重マウント、Selection API）を5回以上にわたって疑い続けたが、
すべて外れだった。**「単語が違う」という時点で外部要因を疑うべきだった** —
アプリのバグでは、文章が別の言い回しに変わることは原理的に起こらない。
ログに最初から出ていた手がかりを、正しく重み付けできていなかった。

原因の切り分けとして有効だったのは、**期待値とDOMの実体を並べて出力する診断**
（`expectedStarts` vs `domStarts`）。仮説を立てて潰すより、両者を並べた瞬間に
「Reactが作っていない要素がある」と一目で分かった。

### この調査で見つかった副次的な問題（いずれも実在し、修正済み）

1. **`quoted_text`だけを保存し、位置を毎回`indexOf`で復元していた** → 同じ文字列が複数
   出現すると常に最初の一致に解決される。`start_offset`/`end_offset`を保存する方式に変更
2. **render中に`let cursor`を加算してセグメント開始位置を求めていた** → Reactの規約違反で
   壊れやすい。純粋関数`computeSegments`の中で確定させる方式に変更
3. **同一会話のこのコンポーネントが同時に3つマウントされていた**（expo-routerの`Stack`が
   前の画面を保持するため）→ 全インスタンスが`document`にリスナーを張り、`querySelector`が
   他インスタンスの要素を掴みうる状態だった。DOMアクセスをインスタンス配下にスコープ
4. **本文が空のとき長さ0のセグメントを返していた** → 不変条件チェックが検出、修正

---

## 2026-07-26（調査記録） — マーカーの表示位置ズレと、位置計算の基盤の堅牢化

> **注意**：この項目は調査中に「アプリ側のバグ」として書いたもの。真因は上の追記のとおり
> ブラウザ拡張機能だった。以下の修正自体はいずれも実在した問題への対処として有効。

### 症状

会話本文でテキストを選択してマーカーを作成すると、**選択した場所とは別の場所**（同じ
文字列が本文中に先に出現する箇所）にマーカーが付いた。マーカー数が多い会話ほど再現しやすく、
色が表示されない・追加直後に画面へ反映されないという形でも現れた。DBへの保存自体は
成功しており（整理待ちの件数は増える）、フロントエンドの表示・位置計算の問題だった。

### 調査中に見つかった、実在した設計上の弱点

（症状の直接の原因ではなかったが、いずれも壊れやすい実装だったため修正した）

**1. 位置を保存せず、毎回テキスト検索で復元していた**

`markers`テーブルは`quoted_text`（引用文字列）だけを保存し、表示位置は描画のたびに
`locateQuotedText`が`content.indexOf(quotedText)`で再検索していた。同じ文字列が会話内に
複数回出現すると常に**最初の一致**に解決されるため、選択した箇所とは違う場所にマーカーが
表示されうる。さらに`computeSegments`の重なり解決（先勝ち）により、既存マーカーと同じ区間に
なった新しいマーカーは描画対象から落ち、色が付かないように見える。

**2. render中に変数を加算してセグメント開始位置を求めていた**

`let cursor`をrender内で宣言し`segments.map()`の中で加算していた。Reactの規約違反で、
React Compiler有効下では特に壊れやすい。

なお、当時観測された「`data-seg-start`がDOM順で単調増加しない（358 → 369 → 363）」は、
この実装のせいだと考えたが**誤りだった**。React Compilerを完全に無効化しても同じ並びが
観測され、最終的にブラウザ拡張機能によるDOM要素の重複・並び替えが原因と判明した。

### 修正

- **位置を永続化**：`markers`に`start_offset`/`end_offset`を追加
  （`20260726000001_marker_offsets.sql`）。Selection APIから作成時点で取得できる正確な
  位置をそのまま保存し、表示時はそれを優先して使う。文字列検索への依存を無くした
- **オフセット計算をrender外の純粋関数へ**：`computeSegments`の戻り値に`start`/`end`を
  追加し、副作用のない関数の中で確定させる。呼び出し側は読み取るだけにした
  （render中の`let cursor`加算を削除）
- **座標変換をセグメント基準に**：`rangeToOffsets`は「セグメントの開始位置＋セグメント内の
  ローカル文字数」で求める。コンテナ全体を一度に数える方式と違い、DOMに依存するのは
  1セグメント内のローカル位置だけになる
- **保存前の検証ガード**：`content.slice(start_offset, end_offset) === quoted_text`が
  一致しない場合は保存自体を中止し、Alertで通知する。誤った位置を黙って保存しない
- **不変条件の自己検査**：`findSegmentInvariantViolations`（純粋関数）で8条件を検査し、
  `computeSegments`が`__DEV__`時のみ`console.assert`で報告する。この検査は導入直後に
  実際のエッジケースバグ（本文が空のとき長さ0のセグメントを返していた）を1件検出した

### 後方互換性

マイグレーション前に作成された既存マーカーは`start_offset`が`null`のため、従来通り
`locateQuotedText`にフォールバックする。**つまり既存マーカーについては、同じ文字列が
複数回出現する場合の位置ズレが残る**（データからは元の意図を復元できないため）。
該当マーカーを編集して保存し直すと、その時点の位置が永続化されて解消する。

### 今後同じバグを検出する仕組み

`findSegmentInvariantViolations`が検査する8条件：

```
□ start >= 0
□ end >= start（start < end として検査。空セグメント禁止も同時に満たす）
□ end <= content.length
□ text === content.slice(start, end)
□ 前セグメントと重複しない
□ startは単調増加
□ endは単調増加
□ 空セグメントを作らない
```

`console.assert`を直接呼ばず違反リストを返す純粋関数にしているのは、**検査自体が壊れた
入力を本当に検出できるかをテストできるようにするため**（検出しない番人は、いない番人より
危険なため）。テストは`src/lib/__tests__/markerLayout.test.ts`にあり、実際に起きた
`358 → 369 → 363`と同じ「開始位置が前へ戻る」パターンも回帰テスト化している。

### 教訓

- **アプリのバグでは、文章が別の言い回しに変わることは起こらない**。「単語が違う」という
  観測が出た時点で、原因はコードの外（拡張機能・プロキシ・別プロセス）だと絞り込めた
- **仮説を立てて潰すより、期待値と実体を並べて出す**。`expectedStarts`と`domStarts`を
  並べた瞬間に「Reactが作っていない要素がある」と一目で分かった
- **DOMをアプリの状態の鏡だと信用しない**。Webでは外部が書き換えうる。DOMから読んだ値は
  必ず検証してから永続化する（保存前ガードがまさにそれで、誤ったデータを1件も残さなかった）
- **派生値を毎回復元するより、確定した時点の値を保存する**。`indexOf`による位置復元は
  同一文字列が複数あると原理的に破綻する
- **render中に外側の変数を書き換えない**。累積値が必要なら純粋関数の中で確定させる
- 壊れた値が静かに下流へ流れると発見が遅れる。**値の生成元で不変条件を検査する**

### 関連コミット

`2cbfb0c`（オフセットの永続化）、`7b2ae4d`（保存前ガード）、
`380de77`（セグメント開始位置を純粋関数へ）、`5e1cdcf`・`dc5c3dc`（不変条件チェック）、
`4c2e01a`（インスタンススコープ）

### 関連ファイル

`src/lib/markerLayout.ts`、`src/lib/domSelection.ts`、
`src/components/conversation-marker-workspace.tsx`、
`supabase/migrations/20260726000001_marker_offsets.sql`
