# CHANGELOG

重要な修正・仕様変更のうち、**コードを読んだだけでは経緯が分からないもの**を記録する。
「なぜこんな作りになっているのか」「なぜこのチェックがあるのか」を半年後の自分が
辿れるようにするためのファイル。日常的な機能追加まで全部書く必要はない。

---

## 2026-07-26 — マーカーの表示位置ズレを修正し、位置計算の基盤を堅牢化

### 症状

会話本文でテキストを選択してマーカーを作成すると、**選択した場所とは別の場所**（同じ
文字列が本文中に先に出現する箇所）にマーカーが付いた。マーカー数が多い会話ほど再現しやすく、
色が表示されない・追加直後に画面へ反映されないという形でも現れた。DBへの保存自体は
成功しており（整理待ちの件数は増える）、フロントエンドの表示・位置計算の問題だった。

### 原因（2つが重なっていた）

**1. 位置を保存せず、毎回テキスト検索で復元していた**

`markers`テーブルは`quoted_text`（引用文字列）だけを保存し、表示位置は描画のたびに
`locateQuotedText`が`content.indexOf(quotedText)`で再検索していた。同じ文字列が会話内に
複数回出現すると常に**最初の一致**に解決されるため、選択した箇所とは違う場所にマーカーが
表示される。さらに`computeSegments`の重なり解決（先勝ち）により、既存マーカーと同じ区間に
なった新しいマーカーは描画対象から落ち、色が付かないように見えていた。

**2. React Compilerとrender中の変数書き換えの組み合わせ**

セグメントの開始位置を、Reactのrender内で宣言した`let cursor`を`segments.map()`の中で
加算しながら求めていた。このプロジェクトはReact Compilerが有効（`app.json`の
`reactCompiler: true`）で、JSXの部分木やコールバックがメモ化され一部だけ再評価されるため、
render中に外側の変数を書き換えるこのパターンでは値が壊れる。実際にDOMへ付与された
`data-seg-start`がDOM順で単調増加しておらず（**358 → 369 → 363**）、これを土台にした
座標計算が本文の別の場所を指していた。

原因1を直しても症状が消えなかったのは原因2があったため。原因2は、DOMに付いていた
`data-seg-start`の並びが不自然であるという指摘から特定できた。

### 修正

- **位置を永続化**：`markers`に`start_offset`/`end_offset`を追加
  （`20260726000001_marker_offsets.sql`）。Selection APIから作成時点で取得できる正確な
  位置をそのまま保存し、表示時はそれを優先して使う。文字列検索への依存を無くした
- **オフセット計算をrender外の純粋関数へ**：`computeSegments`の戻り値に`start`/`end`を
  追加し、副作用のない関数の中で確定させる。呼び出し側は読み取るだけにした
  （render中の`let cursor`加算を削除）
- **座標変換をセグメント基準に**：`rangeToOffsets`は「セグメントの開始位置＋セグメント内の
  ローカル文字数」で求める。コンテナ全体を一度に数える方式と違い、DOMに依存するのは
  1セグメント内のローカル位置だけになる
- **保存前の検証ガード**：`content.slice(start_offset, end_offset) === quoted_text`が
  一致しない場合は保存自体を中止し、Alertで通知する。誤った位置を黙って保存しない
- **不変条件の自己検査**：`findSegmentInvariantViolations`（純粋関数）で8条件を検査し、
  `computeSegments`が`__DEV__`時のみ`console.assert`で報告する。この検査は導入直後に
  実際のエッジケースバグ（本文が空のとき長さ0のセグメントを返していた）を1件検出した

### 後方互換性

マイグレーション前に作成された既存マーカーは`start_offset`が`null`のため、従来通り
`locateQuotedText`にフォールバックする。**つまり既存マーカーについては、同じ文字列が
複数回出現する場合の位置ズレが残る**（データからは元の意図を復元できないため）。
該当マーカーを編集して保存し直すと、その時点の位置が永続化されて解消する。

### 今後同じバグを検出する仕組み

`findSegmentInvariantViolations`が検査する8条件：

```
□ start >= 0
□ end >= start（start < end として検査。空セグメント禁止も同時に満たす）
□ end <= content.length
□ text === content.slice(start, end)
□ 前セグメントと重複しない
□ startは単調増加
□ endは単調増加
□ 空セグメントを作らない
```

`console.assert`を直接呼ばず違反リストを返す純粋関数にしているのは、**検査自体が壊れた
入力を本当に検出できるかをテストできるようにするため**（検出しない番人は、いない番人より
危険なため）。テストは`src/lib/__tests__/markerLayout.test.ts`にあり、実際に起きた
`358 → 369 → 363`と同じ「開始位置が前へ戻る」パターンも回帰テスト化している。

### 教訓

- **render中に外側の変数を書き換えない**。React Compiler有効下では特に壊れる。
  累積値が必要なら純粋関数の中で確定させ、renderは読み取るだけにする
- **派生値を毎回復元するより、確定した時点の値を保存する**。`indexOf`による位置復元は
  同一文字列が複数あると原理的に破綻する
- 壊れた値が静かに下流へ流れると発見が遅れる。**値の生成元で不変条件を検査する**

### 関連コミット

`380de77`（根本原因の修正）、`5e1cdcf`・`dc5c3dc`（不変条件チェック）、
`7b2ae4d`（保存前ガード）、`2cbfb0c`（オフセットの永続化）

### 関連ファイル

`src/lib/markerLayout.ts`、`src/lib/domSelection.ts`、
`src/components/conversation-marker-workspace.tsx`、
`supabase/migrations/20260726000001_marker_offsets.sql`
