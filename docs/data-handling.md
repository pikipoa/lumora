# data-handling.md — データの取り扱い（開発者向け設計契約）

**この文書は法律文書ではない。開発者が守るべき設計契約である。**

プライバシーポリシー・利用規約・FAQ・セキュリティ説明は、すべてこの文書から生成する。
逆に言えば、**この文書に書かれていないデータの流れを実装してはならない。** 新しい外部送信・
新しい保存先・新しいAIプロバイダを追加する時は、先にこの文書を更新すること。

```
Status:        Verified（未確認項目は本文に「未確認」と明記。推測では埋めない）
Created:       2026-07-29
Last Verified: 2026-07-31
Verified By:   ピキさん（リージョン）／Claude Code（ソースコード）
Evidence Source:
  - ソースコード（commit c4bd356 時点の master）
  - Supabase ダッシュボード（リージョン）
  - Anthropic Commercial Terms of Service / プライバシーヘルプセンター（2026-07-31確認・3-1）
```

**この文書は「現在の事実を確認した記録」である。** 内容を更新したら`Last Verified`と
`Evidence Source`も更新すること。逆に、確認していない項目を埋めてはならない——
信頼設計の文書では、完成度100%より**現在の事実100%**が価値を持つ。

関連：`docs/trust-model.md`（この文書を利用者向けに翻訳したもの）、
`PRINCIPLES.md` Principle 3（削除の扱い）、`CLAUDE.md`（外部送信の明示ルール）

---

## 0. 情報フロー（1枚図）

```
  エクスポートファイル（ChatGPT/Claude/Gemini/Perplexity/.md/.jsonl）
        │
        ▼
  ┌─────────────────────────────┐
  │ 端末内（ブラウザ）           │   原本ファイルはここから出ない
  │  パース・正規化              │   Supabase Storageは使用しない
  └──────────┬──────────────────┘
             │  正規化済みテキストのみ
             ▼
  ┌─────────────────────────────┐
  │ Supabase（Postgres）         │   RLSで user_id = auth.uid() のみ
  │  conversations / messages    │
  │  markers / projects / themes │
  └──────────┬──────────────────┘
             │
       ユーザーがマーカーを引く（＝AIに見せる範囲を人間が決める）
             │
             ▼  quoted_text / role_tag / 会話タイトル のみ
  ┌─────────────────────────────┐
  │ Anthropic API（Claude）      │   会話全文は送らない
  └─────────────────────────────┘
```

**AIが読む範囲をユーザー自身が決める**、という構造になっている。これは偶然ではなく、
2026-07-11の情報フロー転換（AIによる自動マーカー発見を廃止し、人間が引いたマーカーだけを
AIの対象にした）の帰結である。最小権限の考え方に相当する。

---

## 1. どこに保存されるか

| データ | 保存先 | 備考 |
|---|---|---|
| エクスポートの**原本ファイル** | **端末内のみ** | クラウドへ送らない。Supabase Storageは使用していない（`src/`全体で`.storage`の使用0件、2026-07-29確認） |
| 会話・メッセージ（正規化済みテキスト） | Supabase Postgres | `conversations` / `messages` |
| Marker（Arca）とその履歴 | Supabase Postgres | `markers` / `marker_history` |
| Realm / Wing / Tag とその割当 | Supabase Postgres | `projects` / `themes` / `tags` / `marker_tags` / `marker_wings` |
| メモ・色の意味・解放フラグ | Supabase Postgres | `memos` / `marker_color_meanings` / `unlock_flags` |
| AI利用量 | Supabase Postgres | `ai_usage_daily`（件数のみ。本文は含まない） |
| アプリ本体の配信 | Vercel | 静的アセット。ユーザーデータは通らない |

**リージョン**：Supabaseプロジェクトは**東京リージョン**（2026-07-31確認）。
つまり**保管されたデータは日本国内に置かれる**。

**アクセス制御**：全テーブルが`user_id`列を直接持ち、RLSは全テーブル共通で
`user_id = auth.uid()`（`data-model.md` 4章）。他ユーザーの行は取得できない。

### 保管と処理は別である（重要）

**「東京リージョンに保管される」と「データが国外へ出ない」は同じではない。**
保管先が国内でも、2章の外部送信が発生した時点でそのデータは国外へ渡りうる。
プライバシーポリシーでは、この2つを混ぜて書かないこと（「日本国内に保管」とだけ書くと、
AI解析時の越境を隠したことになる）。

| | 場所 | 状態 |
|---|---|---|
| **保管**（Supabase Postgres） | 東京リージョン＝日本国内 | 確認済み |
| **AI処理**（Anthropic API） | **地域は未確認**（公開文書に記載が無い。3-1参照） | 「Knowledge Organize」実行時のみ・送るのはマーカーの抜粋のみ。**日本国外の第三者へ送信される**ことは確実 |
| **監視**（Sentry） | **未確認**（Sentryは米国／EUのリージョンを持つ） | 送るのは長さ・ID・エラー情報のみ |
| **配信**（Vercel） | グローバルCDN | ユーザーデータは通らない |

### データ別の3層マトリクス

「どこに保存されるか」「AIはどこまで見るか」「エラー監視には何が渡るか」は**別々の問い**である。
1つの表で同時に答える。

| データ | 保管 | AI処理（Anthropic） | 監視（Sentry） |
|---|---|---|---|
| エクスポートの原本ファイル | **端末内のみ** | 送らない | 送らない |
| 会話本文（`messages.content`） | 東京（Postgres） | **送らない** | 送らない |
| 会話タイトル | 東京 | Organize実行時に送る | 送らない |
| Marker本文（`quoted_text`） | 東京 | Organize実行時に送る | **長さのみ**（`quotedTextLength`） |
| Roleタグ | 東京 | Organize実行時に送る | 送らない |
| Tag名 | 東京 | `organize-wings`時のみ送る | 送らない |
| Realm名・Wing名 | 東京 | 送らない | 送らない |
| メールアドレス | 東京（Supabase Auth） | 送らない | **送らない**（`beforeSend`で削除） |
| `user_id`（uuid） | 東京 | 送らない | 送る（識別のため） |
| エラー・スタックトレース | — | 送らない | 送る |

**読み取れること**：AI処理の列で「送る」になっているのは4行だけで、いずれも
**ユーザーがマーカーを引いた範囲とその文脈**である。会話本文そのものはどの外部サービスにも
渡っていない。

---

## 2. 何が外部へ送られるか

送信先は3つしかない。**これ以外へ送る実装を追加しない。**

### 2-1. Anthropic API（Claude）

Edge Function `organize-markers` / `organize-wings` から呼ぶ。ユーザーが明示的に
「Knowledge Organize」を実行した時のみ発火する（**自動実行しない**）。

| 送るもの | 送らないもの |
|---|---|
| `markers.quoted_text`（マーカーを引いた抜粋） | **会話全文**（`messages.content`） |
| `markers.role_tag` | メールアドレス・アカウント情報 |
| 所属会話のタイトル | 原本ファイル |
| 既存のTag名（`organize-wings`のみ） | 他ユーザーのデータ（RLSで到達不能） |

確認箇所：[`organize-markers/index.ts:152`](../supabase/functions/organize-markers/index.ts)、
[`organize-wings/index.ts:170`](../supabase/functions/organize-wings/index.ts)

#### 外部AI提供者（Anthropic）の規約確認記録

| 項目 | 内容 |
|---|---|
| **確認日** | 2026-07-31 |
| **文書の特定・引用** | Claude Code（公開文書を取得し該当箇所を抽出） |
| **内容の確認・採用判断** | **ピキさん — 実施済み（2026-07-31）**。引用箇所が原文と一致することを本人が確認 |

> 規約が改定された場合、この表の**確認日と確認結果を更新する**こと。更新せずに
> プライバシーポリシーを公開し続けると、「確認した事実」が古いまま根拠になる。

**確認できた事項**

| # | 参照した文書 | 該当箇所 | 確認結果 |
|---|---|---|---|
| 1 | Commercial Terms of Service<br>`anthropic.com/legal/commercial-terms` | Section B, Customer Content | 「Anthropic may not train models on Customer Content from Services.」——**顧客コンテンツでのモデル学習を禁じる旨が明記されている** |
| 2 | 同上 | Section B, Customer Content | 「Customer (a) retains all rights to its Inputs, and (b) owns its Outputs.」——**入力の権利は顧客が保持し、出力は顧客が所有する** |
| 3 | プライバシーヘルプセンター<br>`privacy.claude.com/en/articles/7996866-...` | データ保持期間 | 「For Anthropic API users, we automatically delete inputs and outputs on our backend within 30 days of receipt or generation, except:」——**API利用分は原則30日以内に削除**。例外として、より長い保持を伴うサービス（Files API等）の利用時、個別契約で別途定めた場合、Usage Policy違反の調査（**最長2年**）、法令上の要請 |

> **参照URLの注意**：3の文書は `privacy.anthropic.com` から `privacy.claude.com` へ
> 301リダイレクトされる（2026-07-31時点）。再確認時は移動先で参照すること。

**確認できなかった事項（推測で補完しない）**

| # | 項目 | 状態 |
|---|---|---|
| 1 | **データが処理・保管される地域** | 参照した公開文書のいずれにも記載が無い。**確認できないため記載しない**。以前この文書に「米国で処理される」と書いていたが、これは推測だったため2026-07-31に削除した |
| 2 | Lumoraのアカウントに個別契約・特別なプランが適用されているか | 未確認 |
| 3 | ゼロデータ保持（Zero Data Retention）契約の適用有無 | 未確認 |

**プライバシーポリシーへの書き方（重要）**

**「AI学習に使われません」と断定して書かない。** API利用形態・プラン・契約形態・保持設定・
将来のサービス変更で条件が変わりうるためである。**確認済みの事実だけを、条件付きで書く。**

推奨する書き方の例：

> 当社は、利用する外部AIサービスの提供条件に従い、ユーザー入力データの取り扱いを管理します。

上の表1〜3は「2026-07-31時点で公開されている規約にそう書かれている」という事実であり、
未来にわたる保証ではない。**規約が改定されればこの記録も更新する。**

### 2-2. Sentry（エラー監視）

| 送るもの | 送らないもの |
|---|---|
| エラーメッセージ・スタックトレース | メールアドレス（`beforeSend`で削除） |
| `user.id`（uuid）のみ | 会話本文・マーカー本文 |
| 現在の画面パス（`route`タグ） | コンソール出力（Console統合を除去、breadcrumbも破棄） |
| 明示的に渡した`extra`（例：`quotedTextLength`＝**長さのみ**） | キー名が機微と判定された`extra`（`beforeSend`でスクラブ） |

確認箇所：[`src/lib/sentry.ts`](../src/lib/sentry.ts)。`sendDefaultPii: false`、
`tracesSampleRate: 0`。

**規律**：Sentryへ本文そのものを渡さない。長さ・件数・IDに留める。既存の
`reportPositionMismatch`は`quotedTextLength`（長さ）だけを送っており、これが基準例。

### 2-3. Vercel（ホスティング）

アプリのHTML/JS/CSSを配信する。ユーザーデータはVercelを経由しない
（クライアントからSupabaseへ直接通信する）。アクセスログにIPアドレス等が残る点は
一般的なホスティングと同じ。

---

## 3. 削除の仕様

**ここはPrinciple 3（知識は破壊しない）と混同されやすいので、区別を明確にする。**

Principle 3が禁じているのは**システムによる破壊**であって、**ユーザー自身の削除**ではない。
詳細は`PRINCIPLES.md` Principle 3。

| 操作 | 現在の挙動 | 物理削除 |
|---|---|---|
| マーカーを外す | `status: 'rejected'`（論理削除）。履歴とともに残る | しない |
| Wing/Tagを外す | 中間テーブルの`status`変更 | しない |
| 会話を保留する | `held_at`をセット。一覧から除外 | しない |
| 保留一覧から「完全に削除」 | **物理削除**（2段階の明示操作が必要） | **する** |
| **アカウント削除** | **実装済み**（2026-07-31）。設定画面の最下部 | **する** |

### アカウント削除の範囲（2026-07-31実装）

**Supabase上のデータは全て消える。** `auth.users`の1行を削除すると、`user_id`列を持つ
**全18テーブル**が`on delete cascade`で連鎖削除される（`20260710000001_initial_schema.sql`ほかで
確認済み）。個別のDELETEは書いていない。

> **テーブルを追加する時は、必ず `references auth.users (id) on delete cascade` を付けること。**
> 付け忘れると、そのテーブルだけ削除されずに残る。

削除はEdge Function `delete-account` が行う（`auth.admin.deleteUser`にservice roleが要るため）。
**user_idは引数で受け取らず、JWTから取得したIDのみを削除する** — 自分自身しか削除できない、
というAPI契約である。

**端末内のキャッシュも全消去する**（`src/lib/accountDeletion.ts`の`clearAllLocalCaches`）。
AsyncStorageに加え、Web版では`localStorage`/`sessionStorage`も消す。サーバーの削除が
**成功してから**消す（失敗時に消すと、ログイン状態だけ壊れて復旧できなくなる）。

**削除されないもの**（各サービスの保持ポリシーに従う）：

| | 内容 |
|---|---|
| Sentry | エラー情報に含まれる`user_id`（本文は元々送っていない） |
| Anthropic | 送信済みのマーカー抜粋 |
| Vercel | アクセスログ |

### ギャップ

- **エクスポート機能が無い**（2026-07-31時点）。削除はできるが、**知識を持ち出す手段が無い**。
  `trust-model.md`の「いつでも離れられることは信頼の一部」に照らすと、「離れられる」が
  「捨てるしかない」になっている。削除できないことの方が深刻なため削除を先に実装したが、
  **これは既知の未解消項目である**。削除確認画面には「現在エクスポート機能はありません」と
  明記して隠さない。Phase2で対応する
- 削除後の完全消去までの猶予期間を設けていない（即時削除）

---

## 4. 保持期間

**Lumora側は未定義。** ユーザーが明示的に削除しない限りデータを保持し続ける（アカウント
削除は即時。猶予期間は設けていない）。

**Anthropic側は確認済み**（3-1の確認記録の表・項目3）：API利用分の入力・出力は原則
**30日以内**に削除される。例外はUsage Policy違反の調査（最長2年）・法令上の要請・
個別契約・より長い保持を伴うサービスの利用時。

ローンチ前に次を決めること：

- 自社ポリシーとしての保持期間の方針を明文化するか（現状は「削除するまで保持」）
- Sentryのイベント保持期間（Sentry側の設定に依存）およびSentryプロジェクトのリージョン
- ログの保持期間（Vercel / Supabase）

---

## 5. この文書を更新すべきタイミング

以下のいずれかを行う時は、**実装より先に**この文書を更新する。

- 新しい外部サービスへデータを送る（AIプロバイダの追加を含む）
- 既存の外部送信の内容を増やす（例：会話全文をAIへ送る設計への変更）
- 新しい保存先を追加する（Supabase Storageの利用開始を含む）
- 削除・保持の挙動を変える
- リージョンを変更する

`CLAUDE.md`の「外部送信が発生する箇所は、どのデータがどこに送られるかを明示する」という
ルールの、具体的な適用先がこの文書である。

---

## 付録. 既知の記述の不整合

- ~~**`VISION.md` 198行**：「Supabase（Postgres + Storage）にクラウド保存する」が実態と異なる~~
  → **解消済み（2026-07-31）**。VISION.md側を「Postgres のみ」に訂正し、Storageは
  「画像OCR・PDF・音声・動画の取り込みに着手する場合の将来候補」として表で分離した。
  設計資料は最新状態が唯一の真実であるべきなので、先送りせずその場で直した
